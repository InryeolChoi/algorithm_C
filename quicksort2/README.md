# quicksort2

`quicksort2.c`는 C 표준 라이브러리의 `qsort`를 사용해 정렬하는 예제입니다.

## Files

- `quicksort2.c`: `qsort` 비교 함수와 예제 실행 코드
- `Makefile`: 빌드 및 실행용 Makefile
- `ALGORITHM.md`: `qsort`와 비교 함수 기반 정렬 설명

## Usage

```sh
make
make run
```
